version: "3"
services:
    web:
        image: bheemboy/cards56web2
        container_name: cards56web2
        restart: unless-stopped
        dns:
            - 1.1.1.1
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - ~/acme-challenge:/webapp/wwwroot/.well-known/acme-challenge
